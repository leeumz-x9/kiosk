rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DEVELOPMENT MODE - Allow all access
    // TODO: Change to authenticated users only in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
