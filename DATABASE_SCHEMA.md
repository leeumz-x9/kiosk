# 🗄️ โครงสร้าง Database - ระบบยืดหยุ่น

## 📊 Collections ใน Firestore

### 1. `age_groups` - กำหนดช่วงอายุ (คุณสร้างเอง)

ตารางนี้ให้คุณกำหนดช่วงอายุเองได้ตามต้องการ

```javascript
{
  id: "teens",                    // ID ของกลุ่ม (ต้อง unique)
  name: "วัยรุ่น",                // ชื่อแสดงผล
  ageMin: 13,                     // อายุต่ำสุด
  ageMax: 21,                     // อายุสูงสุด
  description: "เนื้อหาสำหรับวัยรุ่น",
  emoji: "🎓",                    // ไอคอน
  color: "#3b82f6"               // สีประจำกลุ่ม
}
```

#### ตัวอย่างข้อมูลที่ควรมี:

**Document ID: `kids`**
```javascript
{
  id: "kids",
  name: "วัยเด็ก",
  ageMin: 3,
  ageMax: 12,
  description: "เนื้อหาสำหรับเด็ก เช่น ค่าย กิจกรรม เกม",
  emoji: "👶",
  color: "#22c55e"
}
```

**Document ID: `teens`**
```javascript
{
  id: "teens",
  name: "วัยรุ่น",
  ageMin: 13,
  ageMax: 21,
  description: "เนื้อหาสำหรับวัยรุ่น เช่น สาขาวิชา ทุน โปรโมชั่น",
  emoji: "🎓",
  color: "#3b82f6"
}
```

**Document ID: `adults`**
```javascript
{
  id: "adults",
  name: "วัยทำงาน",
  ageMin: 22,
  ageMax: 100,
  description: "เนื้อหาสำหรับผู้ปกครอง เช่น ROI สถิติงาน",
  emoji: "👨‍💼",
  color: "#8b5cf6"
}
```

#### อยากเพิ่มกลุ่มอายุใหม่? เช่น "วัยทอง"

**Document ID: `seniors`**
```javascript
{
  id: "seniors",
  name: "วัยทอง",
  ageMin: 60,
  ageMax: 100,
  description: "เนื้อหาสำหรับผู้สูงอายุ",
  emoji: "👴",
  color: "#f59e0b"
}
```

---

### 2. `content_items` - เนื้อหาที่จะแสดง

```javascript
{
  id: "auto-generated",           // Firebase สร้างให้
  title: "ทุนการศึกษา 100%",      // หัวข้อ
  description: "รับสมัครทุน...",   // คำอธิบาย
  
  type: "scholarship",             // ประเภท (ดูด้านล่าง)
  ageGroupId: "teens",            // ⚠️ ต้องตรงกับ ID ใน age_groups
  
  priority: 10,                    // ลำดับความสำคัญ (1-10)
  tags: ["ทุน", "ฟรี", "100%"],   // แท็ก
  imageUrl: "https://...",         // รูปภาพ (optional)
  
  isActive: true,                  // เปิด/ปิดการแสดงผล
  viewCount: 0,                    // จำนวนครั้งที่ดู
  clickCount: 0,                   // จำนวนครั้งที่คลิก
  
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

#### ⚠️ Field สำคัญ: `ageGroupId`

- **ต้องตรงกับ `id` ใน `age_groups` collection**
- ถ้าใส่ `"teens"` → จะแสดงให้คนอายุ 13-21 ปี
- ถ้าใส่ `"kids"` → จะแสดงให้คนอายุ 3-12 ปี
- ถ้าใส่ `"seniors"` → จะแสดงให้คนอายุ 60-100 ปี (ถ้าสร้างไว้)

#### ประเภทเนื้อหา (type):

- `scholarship` - ทุนการศึกษา
- `news` - ข่าวสาร
- `event` - กิจกรรม
- `promotion` - โปรโมชั่น
- `career` - สาขาวิชา
- `activity` - กิจกรรมนักศึกษา

---

### 3. `content_types` - ประเภทเนื้อหา (อ่านอย่างเดียว)

```javascript
{
  id: "scholarship",
  name: "ทุนการศึกษา",
  icon: "🎓",
  color: "#10b981"
}
```

---

### 4. `user_interactions` - Log การใช้งาน

```javascript
{
  sessionId: "session-xxx",
  age: 18,
  gender: "male",
  emotion: "happy",
  contentId: "content-xxx",
  action: "view",              // "view" หรือ "click"
  timestamp: Timestamp
}
```

---

## 🔄 การทำงานของระบบ

### 1. ผู้ใช้อายุ 18 ปี เข้ามาใช้เครื่อง

```
1. สแกนหน้า → ตรวจพบอายุ 18 ปี
   ↓
2. ระบบ query `age_groups` → หาว่า 18 อยู่ในช่วงไหน
   → พบ "teens" (13-21 ปี) ✓
   ↓
3. ระบบ query `content_items`:
   WHERE ageGroupId == "teens"
   AND isActive == true
   ORDER BY priority DESC
   LIMIT 20
   ↓
4. แสดงป๊อปอัพเนื้อหาที่ตรงกับ "teens"
```

### 2. ผู้ใช้อายุ 65 ปี เข้ามาใช้เครื่อง

```
1. สแกนหน้า → ตรวจพบอายุ 65 ปี
   ↓
2. ระบบ query `age_groups` → หาว่า 65 อยู่ในช่วงไหน
   → ถ้ามี "seniors" (60-100) → ใช้ "seniors" ✓
   → ถ้าไม่มี → ใช้กลุ่มสุดท้าย (adults)
   ↓
3. ระบบ query `content_items`:
   WHERE ageGroupId == "seniors"  (หรือ "adults")
   AND isActive == true
   ORDER BY priority DESC
   LIMIT 20
   ↓
4. แสดงป๊อปอัพเนื้อหาที่ตรงกับช่วงอายุนั้น
```

---

## ✨ ข้อดีของระบบนี้

### 1. ยืดหยุ่น - กำหนดช่วงอายุเองได้
```
อยากแบ่ง 5 กลุ่ม? ได้!
- วัยเด็กเล็ก (3-7)
- วัยเด็กโต (8-12)
- วัยรุ่นตอนต้น (13-15)
- วัยรุ่นตอนปลาย (16-21)
- วัยทำงาน (22-100)
```

### 2. แก้ไขง่าย
```
อยากเปลี่ยนช่วงอายุวัยรุ่นเป็น 14-22?
→ แค่แก้ไขใน age_groups collection
→ ไม่ต้องแก้โค้ด!
```

### 3. เพิ่มกลุ่มใหม่ได้ทันที
```
อยากเพิ่มกลุ่ม "วัยมัธยม" (13-15)?
→ เพิ่ม document ใหม่ใน age_groups
→ เพิ่มเนื้อหาใน content_items ที่ ageGroupId = "วัยมัธยม"
→ เสร็จ! ระบบจะทำงานทันที
```

---

## 📝 วิธีเพิ่มข้อมูล

### วิธีที่ 1: Firebase Console (ง่ายที่สุด)

#### เพิ่มช่วงอายุ:
1. เข้า Firebase Console → Firestore Database
2. เลือก Collection `age_groups`
3. Add Document:
   - Document ID: `highschool`
   - Fields:
     ```
     id: "highschool" (string)
     name: "วัยมัธยม" (string)
     ageMin: 13 (number)
     ageMax: 15 (number)
     description: "เนื้อหาสำหรับนักเรียนมัธยม" (string)
     emoji: "🎒" (string)
     color: "#f59e0b" (string)
     ```

#### เพิ่มเนื้อหา:
1. เลือก Collection `content_items`
2. Add Document (Auto-ID):
   - Fields:
     ```
     title: "ทุนนักเรียนมัธยม" (string)
     description: "รับสมัครทุน..." (string)
     type: "scholarship" (string)
     ageGroupId: "highschool" (string) ⚠️ ต้องตรง!
     priority: 8 (number)
     tags: ["ทุน", "มัธยม"] (array)
     imageUrl: "" (string)
     isActive: true (boolean)
     viewCount: 0 (number)
     clickCount: 0 (number)
     createdAt: [timestamp - now]
     updatedAt: [timestamp - now]
     ```

### วิธีที่ 2: Admin Panel

1. กด Ctrl + Shift + A (เปิด Admin Panel)
2. กด "➕ เพิ่มเนื้อหาใหม่"
3. กรอกฟอร์ม:
   - กลุ่มอายุ → เลือกจาก dropdown (จะโหลดจาก age_groups อัตโนมัติ)
   - ประเภท → เลือกประเภทเนื้อหา
   - Priority → 1-10
4. บันทึก

### วิธีที่ 3: Script (จำนวนมาก)

แก้ไข `seed-content.js` แล้วรัน:
```bash
npm run seed
```

---

## 🎯 ตัวอย่างการใช้งานจริง

### สถานการณ์: อยากแบ่งวัยรุ่นเป็น 2 กลุ่ม

#### 1. สร้างกลุ่มอายุใหม่

**กลุ่ม 1: วัยรุ่นตอนต้น (13-16)**
```javascript
{
  id: "teens_early",
  name: "วัยรุ่นตอนต้น",
  ageMin: 13,
  ageMax: 16,
  emoji: "🎒",
  color: "#3b82f6"
}
```

**กลุ่ม 2: วัยรุ่นตอนปลาย (17-21)**
```javascript
{
  id: "teens_late",
  name: "วัยรุ่นตอนปลาย",
  ageMin: 17,
  ageMax: 21,
  emoji: "🎓",
  color: "#8b5cf6"
}
```

#### 2. เพิ่มเนื้อหาแยกตามกลุ่ม

**สำหรับวัยรุ่นตอนต้น:**
```javascript
{
  title: "แนะแนวเลือกสาขาวิชา",
  description: "เริ่มคิดว่าอยากเรียนอะไรดี?",
  ageGroupId: "teens_early",  // 13-16 ปี
  priority: 9
}
```

**สำหรับวัยรุ่นตอนปลาย:**
```javascript
{
  title: "สมัครเรียนต่อ ปวช./ปวส.",
  description: "พร้อมสมัครแล้ว! มาดูสาขากันเลย",
  ageGroupId: "teens_late",   // 17-21 ปี
  priority: 10
}
```

#### 3. เสร็จ!

- ผู้ใช้อายุ 14 → เห็นเนื้อหา "teens_early"
- ผู้ใช้อายุ 19 → เห็นเนื้อหา "teens_late"
- **ไม่ต้องแก้โค้ดเลย!**

---

## 🔍 Query ที่ระบบใช้

### 1. ดึงกลุ่มอายุทั้งหมด
```javascript
const ageGroups = await getDocs(collection(db, 'age_groups'));
// Cache ไว้ 5 นาที
```

### 2. หากลุ่มที่ตรงกับอายุ
```javascript
const matchedGroup = ageGroups.find(group => 
  age >= group.ageMin && age <= group.ageMax
);
```

### 3. ดึงเนื้อหาตามกลุ่มอายุ
```javascript
const q = query(
  collection(db, 'content_items'),
  where('ageGroupId', '==', matchedGroup.id),
  where('isActive', '==', true),
  orderBy('priority', 'desc'),
  orderBy('createdAt', 'desc'),
  limit(20)
);
```

---

## ⚠️ ข้อควรระวัง

### 1. `ageGroupId` ต้องตรงกับ `id` ใน age_groups
```
❌ ผิด:
content_items → ageGroupId: "teenagers"
age_groups → id: "teens"

✅ ถูก:
content_items → ageGroupId: "teens"
age_groups → id: "teens"
```

### 2. ช่วงอายุไม่ควรทับซ้อน
```
❌ ไม่ควร:
Group A: 13-18
Group B: 16-21  (ทับที่ 16-18)

✅ ควร:
Group A: 13-15
Group B: 16-21
```

### 3. ควรมีกลุ่ม default สำหรับอายุที่ไม่ตรง
```
ระบบจะเลือกกลุ่มสุดท้าย (ageMax สูงสุด) เป็น default
```

---

## 🚀 สรุป

1. **ยืดหยุ่น**: กำหนดช่วงอายุได้เอง
2. **ไม่ต้องแก้โค้ด**: เพิ่มกลุ่มใหม่ผ่าน Firebase
3. **Dynamic**: ระบบดึงข้อมูลแบบ realtime
4. **Scalable**: เพิ่มเนื้อหาได้ไม่จำกัด
5. **Performance**: มี cache ไม่หนัก

**พร้อมใช้งานแล้ว! 🎉**
