# 📚 คู่มือการใช้งานระบบ Content Management

## 🎯 ภาพรวมระบบ

ระบบนี้ออกแบบมาเพื่อ:
1. **สแกนหน้า** → แสดงอายุ, เพศ, อารมณ์
2. **แสดงป๊อปอัพหลัง 10 วินาที** → เนื้อหาที่เหมาะสมตามอายุ
3. **แสดงต่อเนื่อง** → Auto slide ทุก 8 วินาที
4. **เก็บสถิติ** → Views และ Clicks

## 🔧 การติดตั้ง

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่า Firebase
แก้ไข `src/config.js`:
```javascript
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  // ... ข้อมูลอื่นๆ
};
```

### 3. Deploy Firestore Rules
```bash
firebase deploy --only firestore:rules
```

### 4. เพิ่มข้อมูลตัวอย่าง (Optional)

#### วิธีที่ 1: ใช้ Script (แนะนำ)
```bash
# 1. Download Service Account Key จาก Firebase Console
# Project Settings → Service Accounts → Generate New Private Key
# บันทึกเป็น serviceAccountKey.json

# 2. ติดตั้ง firebase-admin
npm install firebase-admin

# 3. รัน script
node seed-content.js
```

#### วิธีที่ 2: ใช้ Admin Panel
1. เปิดแอป: `npm run dev`
2. กด Ctrl + Shift + A เพื่อเปิด Admin Panel
3. เพิ่มข้อมูลผ่านฟอร์ม

#### วิธีที่ 3: ใช้ Firebase Console
1. เข้า Firebase Console → Firestore Database
2. สร้าง Collection ชื่อ `content_items`
3. เพิ่ม Document ด้วยโครงสร้างตาม CONTENT_STRUCTURE.md

## 📋 โครงสร้างข้อมูล

### Collection: `content_items`
```javascript
{
  title: "หัวข้อ",
  description: "รายละเอียด",
  type: "scholarship", // scholarship, news, event, promotion, career, activity
  ageGroup: "teens", // kids, teens, adults
  priority: 5, // 1-10 (สูงกว่าแสดงก่อน)
  tags: ["tag1", "tag2"],
  imageUrl: "https://...",
  isActive: true,
  viewCount: 0,
  clickCount: 0,
  createdAt: Timestamp,
  updatedAt: Timestamp
}
```

### ช่วงอายุ (Age Groups)
- **kids** (3-12 ปี): วัยเด็ก - เนื้อหาสนุก เล่นเกม กิจกรรม
- **teens** (13-21 ปี): วัยรุ่น - สาขาวิชา ทุน โปรโมชั่น กิจกรรม
- **adults** (22+ ปี): วัยทำงาน/ผู้ปกครอง - ROI สถิติงาน ข้อมูลเปรียบเทียบ

### ประเภทเนื้อหา (Content Types)
- `scholarship` 🎓 ทุนการศึกษา
- `news` 📰 ข่าวสาร
- `event` 🎉 กิจกรรม
- `promotion` 🎁 โปรโมชั่น
- `career` 💼 สาขาวิชา
- `activity` ⚽ กิจกรรมนักศึกษา

## 🎨 การใช้งาน Admin Panel

### เปิด Admin Panel
```javascript
// กด Ctrl + Shift + A หรือแก้ไข AdminMenu.jsx เพิ่มปุ่ม
```

### ฟังก์ชั่น
1. **➕ เพิ่มเนื้อหาใหม่**
   - กรอกฟอร์ม
   - เลือกประเภทและกลุ่มอายุ
   - กำหนด Priority (1-10)
   - เพิ่ม Tags คั่นด้วยจุลภาค
   - บันทึก

2. **✏️ แก้ไขเนื้อหา**
   - คลิกปุ่ม "แก้ไข" ที่การ์ด
   - แก้ไขข้อมูล
   - บันทึกการแก้ไข

3. **🗑️ ลบเนื้อหา**
   - คลิกปุ่ม "ลบ"
   - ยืนยันการลบ

4. **✅/❌ เปิด/ปิดใช้งาน**
   - คลิกปุ่ม Toggle
   - เนื้อหาที่ปิดจะไม่แสดงในป๊อปอัพ

## 🚀 การทำงานของระบบ

### Flow
```
1. User เข้ามา
   ↓
2. สแกนหน้า → แสดง อายุ, เพศ, อารมณ์
   ↓
3. รอ 10 วินาที
   ↓
4. แสดงป๊อปอัพ (ดึงข้อมูลจาก Firestore ตาม ageGroup)
   ↓
5. Auto slide ทุก 8 วินาที
   ↓
6. บันทึก Views/Clicks
   ↓
7. วนลูปจนกว่า user จะเดินจากเครื่อง
```

### การดึงข้อมูล
```javascript
// ดึงข้อมูลตามอายุ (แบบ One-time Fetch)
const contents = await getContentByAge(age, 20);

// Query:
// - ageGroup == "teens"
// - isActive == true
// - order by priority DESC, createdAt DESC
// - limit 20
```

### Performance Optimization
- **One-time Fetch**: ไม่ใช้ Real-time Listener (ประหยัดทรัพยากร)
- **Limit 20**: ดึงไม่เกิน 20 รายการต่อครั้ง
- **Cache**: เก็บข้อมูลใน state ไม่ query ซ้ำ
- **Lazy Loading**: โหลดรูปตอนแสดงจริง

## 💡 Best Practices

### 1. การจัดการเนื้อหา
- **Priority สูง (8-10)**: ข้อมูลสำคัญ เช่น ทุน โปรโมชั่น
- **Priority กลาง (5-7)**: เนื้อหาทั่วไป
- **Priority ต่ำ (1-4)**: ข้อมูลรอง

### 2. การเขียน Description
- **วัยเด็ก**: ภาษาง่าย สั้น มีอิโมจิ
- **วัยรุ่น**: ภาษาวัยรุ่น สดใส ตรงประเด็น
- **วัยทำงาน**: ภาษาทางการ มีข้อมูลสถิติ ROI

### 3. การใช้ Tags
- ใช้คำค้นหาที่เกี่ยวข้อง
- คั่นด้วยจุลภาค
- ภาษาไทยและอังกฤษ
- ตัวอย่าง: `ทุน, การศึกษา, ฟรี, scholarship`

### 4. การใส่รูปภาพ
- ใช้ URL จาก CDN หรือ Firebase Storage
- ขนาดแนะนำ: 800x600px
- รูปสวย คมชัด เข้าเรื่อง

## 🔐 Security

### Firestore Rules (ปัจจุบัน)
```javascript
// ⚠️ ยังไม่มี Authentication
allow create, update, delete: if true;
```

### แนะนำ (Production)
```javascript
// ใช้ Firebase Authentication
allow write: if request.auth != null && 
  request.auth.token.admin == true;
```

## 📊 Analytics

### ข้อมูลที่เก็บ
- `viewCount`: จำนวนครั้งที่แสดง
- `clickCount`: จำนวนครั้งที่คลิก
- `user_interactions`: บันทึกการใช้งานแต่ละ session

### ดู Analytics
```javascript
// Query top viewed content
const q = query(
  collection(db, 'content_items'),
  orderBy('viewCount', 'desc'),
  limit(10)
);
```

## 🐛 Troubleshooting

### ป๊อปอัพไม่แสดง
1. ตรวจสอบ `detectedAge` มีค่าหรือไม่
2. ดู Console มี error หรือไม่
3. ตรวจสอบ Firestore Rules
4. ดูว่ามีข้อมูลใน `content_items` หรือไม่

### ข้อมูลไม่ขึ้น
1. ตรวจสอบการตั้งค่า Firebase
2. Deploy Firestore Rules: `firebase deploy --only firestore:rules`
3. ตรวจสอบ `isActive = true`
4. ตรวจสอบ `ageGroup` ถูกต้อง

### รูปภาพไม่โหลด
1. ตรวจสอบ URL ถูกต้อง
2. ใช้ HTTPS
3. ตรวจสอบ CORS
4. ลองใช้ Firebase Storage แทน

## 📝 TODO / Future

- [ ] เพิ่ม Firebase Authentication
- [ ] Admin Panel แยกหน้า
- [ ] Upload รูปผ่าน Firebase Storage
- [ ] Export/Import ข้อมูล CSV
- [ ] Multi-language support
- [ ] Scheduled Content (เผยแพร่ตามเวลา)
- [ ] A/B Testing
- [ ] Advanced Analytics Dashboard

## 📞 Support

หากมีปัญหาหรือข้อสงสัย:
1. ดู Error ใน Console
2. ตรวจสอบ Firestore Database
3. อ่าน CONTENT_STRUCTURE.md
4. ทดสอบด้วยข้อมูล Fallback
